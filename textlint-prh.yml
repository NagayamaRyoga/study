version: 1
rules:
  - expected: かかわらず
    patterns:
      - /関わらず/
  - expected: 埋め込み
    patterns:
      - /埋込み/
      - /埋込/
  - expected: 埋め込み器
    patterns:
      - /埋め込み機/
  - expected: 抽出器
    patterns:
      - /抽出機/
